// Operadores Aritiméticos 

/* são operadores binários, operam em cima de 2 operandos*/

const [a, b, c, d] = [3, 5, 1 ,15] // usando o operador destructuring para criar as constantes

const soma = a + b + c + d // sempre operando de forma binária a + b

// ++d, d++, d +d > são os operadores prefixe, postfixe e infixe  os 2 primeiros são unários

const subtracao = d - c // subtração
const multiplicacao = a * b // multiplicação
const divisao = a / b // divisão
const modulo = a % 2 // que é o resultado do resto da divisão por 2 

console.log(soma, subtracao, multiplicacao, divisao, modulo)




